# Copy this to .env and edit values for local dev
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=password
MONGO_DB=appdb

# Elasticsearch (dev with security disabled in compose above)
ELASTIC_URL=http://localhost:9200

# Redis
REDIS_PASSWORD=changeme
